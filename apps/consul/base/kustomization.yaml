apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

namespace: service-checker

labels:
  - pairs:
      app: consul
      component: service-discovery
      managed-by: argocd