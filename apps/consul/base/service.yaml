apiVersion: v1
kind: Service
metadata:
  name: consul
spec:
  type: ClusterIP
  selector:
    app: consul
  ports:
  - port: 8500
    targetPort: 8500
    name: http
  - port: 8600
    targetPort: 8600
    name: dns
    protocol: UDP