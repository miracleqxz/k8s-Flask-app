apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - daemonset.yaml
  - configmap.yaml
  - serviceaccount.yaml

namespace: service-checker

commonLabels:
  app: promtail
  component: logging
  managed-by: argocd