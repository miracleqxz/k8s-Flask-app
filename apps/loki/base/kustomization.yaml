apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - statefulset.yaml
  - service.yaml
  - configmap.yaml

namespace: service-checker

commonLabels:
  app: loki
  component: logging
  managed-by: argocd