apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flask-ingress.yaml

namespace: service-checker

commonLabels:
  app: ingress
  component: networking
  managed-by: argocd