apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

namespace: service-checker

commonLabels:
  app: grafana
  component: monitoring
  managed-by: argocd