apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

namespace: service-checker

labels:
  - pairs:
      app: alertmanager
      component: monitoring
      managed-by: argocd